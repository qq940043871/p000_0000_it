\n\n小型计算机完整电路连接图

@import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@300;400;700&family=Noto+Sans+SC:wght@300;400;500;600&display=swap');
:root{
  --bg:#0a0c12;--s1:#0f1118;--s2:#141720;--bd:#1e2235;
  --cpu:#ff6b35;--mem:#00d4ff;--disk:#4ade80;--io:#a78bfa;
  --clk:#fbbf24;--bus:#f472b6;--pwr:#34d399;
  --tx:#e2e8f0;--d1:#94a3b8;--d2:#64748b;
}
*{margin:0;padding:0;box-sizing:border-box;}
body{background:var(--bg);color:var(--tx);font-family:'Noto Sans SC',sans-serif;font-weight:300;line-height:1.8;}
.wrap{max-width:960px;margin:0 auto;padding:30px 16px 80px;}

/* Header */
.hd{text-align:center;padding:40px 0 30px;}
.hd-tag{font-family:'JetBrains Mono',monospace;font-size:10px;letter-spacing:3px;color:var(--cpu);margin-bottom:14px;}
.hd h1{font-size:clamp(22px,4vw,36px);font-weight:600;margin-bottom:10px;}
.hd-sub{font-size:13px;color:var(--d1);}

/* Section */
.sec{margin:30px 0;}
.sec-hd{display:flex;align-items:center;gap:10px;margin-bottom:18px;}
.sec-dot{width:8px;height:8px;border-radius:50%;flex-shrink:0;}
.sec h2{font-size:16px;font-weight:500;}
.sec-line{flex:1;height:1px;background:linear-gradient(90deg,var(--bd),transparent);}

/* Card */
.card{background:var(--s1);border:1px solid var(--bd);border-radius:10px;padding:20px;margin-bottom:16px;}
.card-title{font-family:'JetBrains Mono',monospace;font-size:10px;letter-spacing:2px;text-transform:uppercase;margin-bottom:14px;display:flex;align-items:center;gap:8px;}
.card-title::before{content:'';width:3px;height:14px;border-radius:2px;display:block;}

/* Code block */
.code{background:#050608;border:1px solid var(--bd);border-radius:8px;overflow:hidden;margin:14px 0;}
.code-hd{display:flex;align-items:center;justify-content:space-between;padding:7px 14px;border-bottom:1px solid var(--bd);}
.code-dots{display:flex;gap:5px;}
.code-dots span{width:8px;height:8px;border-radius:50%;}
.code-dots span:nth-child(1){background:#ff5f57}
.code-dots span:nth-child(2){background:#febc2e}
.code-dots span:nth-child(3){background:#28c840}
.code-lbl{font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--d2);letter-spacing:1px;}
.code-bd{padding:14px 16px;font-family:'JetBrains Mono',monospace;font-size:11.5px;line-height:1.9;overflow-x:auto;white-space:pre;}

/* Pin table */
.tbl{width:100%;border-collapse:collapse;font-size:12.5px;margin:10px 0;}
.tbl th{font-family:'JetBrains Mono',monospace;font-size:9px;letter-spacing:1px;text-transform:uppercase;color:var(--d2);padding:7px 10px;text-align:left;border-bottom:1px solid var(--bd);background:var(--s2);}
.tbl td{padding:8px 10px;border-bottom:1px solid rgba(30,34,53,.5);color:var(--d1);vertical-align:top;font-size:12px;}
.tbl tr:last-child td{border-bottom:none;}
.pin{font-family:'JetBrains Mono',monospace;font-size:11px;font-weight:700;}
.cpu{color:var(--cpu);}.mem{color:var(--mem);}.disk{color:var(--disk);}
.io{color:var(--io);}.clk{color:var(--clk);}.bus{color:var(--bus);}.pwr{color:var(--pwr);}

/* Diagram */
.diag{background:#050608;border:1px solid var(--bd);border-radius:8px;padding:16px;font-family:'JetBrains Mono',monospace;font-size:11px;line-height:1.8;overflow-x:auto;white-space:pre;}

/* Note */
.note{background:rgba(251,191,36,.05);border:1px solid rgba(251,191,36,.2);border-radius:8px;padding:14px 16px;font-size:12.5px;color:var(--d1);margin:14px 0;}
.note strong{color:var(--clk);}

/* Grid */
.grid2{display:grid;grid-template-columns:1fr 1fr;gap:14px;}
@media(max-width:600px){.grid2{grid-template-columns:1fr;}}

/* Divider */
.div{height:1px;background:linear-gradient(90deg,transparent,var(--bd),transparent);margin:36px 0;}

/* Step */
.step{display:flex;gap:14px;margin-bottom:16px;}
.step-num{font-family:'JetBrains Mono',monospace;font-size:11px;font-weight:700;width:26px;height:26px;border-radius:50%;display:flex;align-items:center;justify-content:center;flex-shrink:0;margin-top:2px;}
.step-body{flex:1;}
.step-title{font-size:14px;font-weight:500;color:#fff;margin-bottom:4px;}
.step-desc{font-size:12.5px;color:var(--d1);line-height:1.7;}

.foot{text-align:center;padding:40px 0 10px;font-family:'JetBrains Mono',monospace;font-size:9px;color:var(--d2);letter-spacing:2px;border-top:1px solid var(--bd);margin-top:50px;}\n\n  HARDWARE CIRCUIT GUIDE
  小型计算机完整电路连接图
  CPU · 内存 · 硬盘 · 键盘鼠标 · 时钟 · 总线 — 从零搭建一台完整的计算机\n\n  总体架构图
                      ┌─────────────┐
                    │  晶振 XTAL  │  ← 时钟源（如 25MHz）
                    └──────┬──────┘
                           │ CLK
  ┌──────────┐            │            ┌──────────────┐
  │  电源    │            │            │  BIOS/UEFI   │
  │  ATX PSU │            │            │  ROM 芯片    │
  │  +5V +12V│            │            └──────┬───────┘
  └────┬─────┘            │                   │
       │ VCC              │                   │ 地址总线
       │         ┌────────┴────────┐          │
       └────────►│                 │◄─────────┘
                 │   CPU (核心)    │
                 │  ┌───────────┐  │
                 │  │ ALU 运算  │  │
                 │  │ 寄存器组  │  │
                 │  │ 控制单元  │  │
                 │  └───────────┘  │
                 └────────┬────────┘
                          │
         ┌────────────────┼────────────────┐
         │                │                │
    地址总线(AB)      数据总线(DB)      控制总线(CB)
    Address Bus       Data Bus          Control Bus
    (单向→)           (双向↔)           (双向↔)
         │                │                │
         └────────────────┼────────────────┘
                          │
         ┌────────────────┼────────────────┐
         │                │                │
  ┌──────┴──────┐          │         ┌──────┴──────┐
  │  RAM 内存   │          │         │  ROM/Flash  │
  │  DIMM 插槽  │          │         │  固件存储   │
  └─────────────┘          │         └─────────────┘
                          │
         ┌────────────────┼────────────────┐
         │                │                │
  ┌──────┴──────┐  ┌──────┴──────┐  ┌──────┴──────┐
  │  硬盘/SSD   │  │  键盘控制器 │  │  鼠标控制器 │
  │  SATA/NVMe  │  │  PS/2 或USB │  │  PS/2 或USB │
  └─────────────┘  └─────────────┘  └─────────────┘\n\n  CPU 核心电路

  
    CPU 关键引脚说明
    
      引脚组名称方向说明
      
        CLK时钟输入← 输入接晶振，决定 CPU 运行速度
        RESET复位← 输入低电平触发，CPU 重新启动
        A[0:31]地址总线→ 输出32根线，寻址 4GB 内存空间
        D[0:63]数据总线↔ 双向64根线，读写数据
        RD/WR读写控制→ 输出高=读，低=写
        INT中断请求← 输入硬件设备发出中断信号
        INTA中断应答→ 输出CPU 确认收到中断
        VCC/GND电源/地← 输入+3.3V 或 +1.2V 核心电压
      
    
  

  
    CPU 时钟电路连接
    晶振 (25MHz)
    ├── 引脚1 → CPU.CLK        ← 时钟信号
    ├── 引脚2 → GND            ← 接地
    └── 旁路电容 22pF → GND    ← 滤波稳定

复位电路 (RC复位)
    VCC ──┬── 10kΩ ──┬── CPU.RESET
          │          │
         100nF       └── 按钮 ── GND
          │
         GND

说明：上电时电容充电，RESET 先低后高，CPU 完成复位
  \n\n  内存 (RAM) 电路

  
    SRAM 静态内存引脚（简单版，适合学习）
    
      引脚名称连接到说明
      
        A[0:14]地址线CPU 地址总线15根，寻址 32KB
        D[0:7]数据线CPU 数据总线8根，8位数据
        CS̄片选（低有效）地址译码器输出选中该内存芯片
        OE̅输出使能CPU.RD读操作时拉低
        WE̅写使能CPU.WR写操作时拉低
        VCC/GND电源+5V / GNDSRAM 用 5V
      
    
  

  
    内存连接电路
    CPU                    SRAM (如 62256)
────                   ──────────────
A[0:14]  ──────────►  A[0:14]     (地址线直连)
D[0:7]   ◄──────────► D[0:7]      (数据线双向)
RD       ──────────►  OE̅          (读使能)
WR       ──────────►  WE̅          (写使能)

地址译码器 (74HC138)
────────────────────
CPU.A[15:17] → 译码器输入
译码器输出0  → SRAM.CS̄    (选中内存)
译码器输出1  → ROM.CS̄     (选中ROM)
译码器输出2  → IO.CS̄      (选中IO设备)

说明：地址译码器决定哪个设备被选中，避免冲突
  

  💡 现代 DDR 内存：原理相同，但引脚更多（240+），有时钟同步、差分信号等，通常直接用主板 DIMM 插槽，不需要手动连线。\n\n  硬盘/存储 电路

  
    
      SATA 硬盘接口
      
        引脚说明
        
          TX+/TX-发送差分信号
          RX+/RX-接收差分信号
          +5V逻辑电路供电
          +12V马达供电（机械盘）
          GND接地（多个）
        
      
    
    
      NVMe SSD (M.2 接口)
      
        信号说明
        
          PCIe x44条高速通道
          PERST#复位信号
          +3.3V供电
          CLKREQ#时钟请求
          PEWAKE#唤醒信号
        
      
    
  

  
    SATA 连接示意
    主板 SATA 控制器          硬盘
──────────────           ────
TX+ ──────────────────► RX+
TX- ──────────────────► RX-
RX+ ◄────────────────── TX+
RX- ◄────────────────── TX-

电源接口 (SATA 电源线)
+3.3V ─── 引脚 1-3
+5V   ─── 引脚 4-6
+12V  ─── 引脚 10-12
GND   ─── 引脚 7-9

注：SATA 用差分信号，抗干扰强，速度可达 6Gbps
  \n\n  键盘 & 鼠标 电路

  
    
      PS/2 接口（经典）
      
        引脚颜色说明
        
          1DATA数据线（双向）
          3GND接地
          4VCC+5V 供电
          5CLK时钟线（键盘产生）
        
      
    
    
      USB 接口（现代）
      
        引脚颜色说明
        
          1红VBUS +5V 供电
          2白D- 数据负
          3绿D+ 数据正
          4黑GND 接地
        
      
    
  

  
    键盘中断连接
    键盘控制器 (8042)
    ├── PS/2.DATA  ← 键盘数据
    ├── PS/2.CLK   ← 键盘时钟
    ├── IRQ1       → 中断控制器 (APIC)
    └── I/O 端口 0x60/0x64 ← CPU 读取扫描码

中断流程：
按键 → 键盘产生扫描码 → 发给8042 → 8042拉高IRQ1
→ APIC通知CPU → CPU执行中断处理程序
→ 从0x60端口读取扫描码 → 翻译成ASCII
  \n\n  时钟系统（最关键！）

  ⚡ 为什么时钟最重要？ 计算机里所有操作都要"踩点"进行，时钟就是那个节拍器。没有时钟，CPU 不知道什么时候执行下一条指令，内存不知道什么时候锁存数据。

  
    完整时钟分配系统
    晶振 (25MHz 基准)
    │
    ▼
PLL 锁相环 (倍频/分频)
    ├── ×100 → 2500MHz → CPU 核心时钟
    ├── ×4  → 100MHz  → 内存总线时钟
    ├── ÷1  → 25MHz   → PCIe 参考时钟
    └── ÷25 → 1MHz    → 定时器/RTC

内存时钟 (DDR4 示例)
    CLK+/CLK- → 差分时钟，直连内存条
    频率: 3200MHz (实际传输速率)

PCIe 时钟
    REFCLK+/REFCLK- → 差分，100MHz
    → 硬盘/显卡/网卡 共用

RTC 实时时钟
    32.768kHz 晶振 → RTC 芯片
    纽扣电池供电，断电也走时
  

  
    时钟信号连接规则
    
      规则原因
      
        走线要短且等长避免时序偏差（skew）
        加旁路电容（100nF）滤除高频噪声
        高速时钟用差分信号抗干扰，传输更远
        时钟线不能打折阻抗不连续会反射
        时钟线旁边铺地屏蔽干扰
      
    
  \n\n  完整连接步骤（从零开始）

  
    1
    
      先连电源
      ATX 电源 → 主板 24pin 接口。CPU 单独 8pin 供电。硬盘 SATA 电源线。所有 GND 必须共地！
    
  

  
    2
    
      连时钟
      晶振 → PLL → CPU CLK 引脚。这是最先要确认的，没有时钟 CPU 不工作。
    
  

  
    3
    
      连复位电路
      RC 复位电路 → CPU RESET 引脚。确保上电时 CPU 能正确初始化。
    
  

  
    4
    
      连内存
      CPU 地址总线 → 内存地址引脚。CPU 数据总线 ↔ 内存数据引脚。地址译码器决定内存映射范围。
    
  

  
    5
    
      连存储（硬盘/ROM）
      BIOS ROM → 地址总线（固定地址 0xFFFFFFF0）。硬盘 → SATA 控制器 → PCIe 总线 → CPU。
    
  

  
    6
    
      连 I/O 设备
      键盘/鼠标 → USB 控制器 → PCIe → CPU。中断线 IRQ → APIC → CPU INT 引脚。
    
  

  
    7
    
      验证总线
      用示波器检查时钟波形是否正常。用逻辑分析仪检查总线数据。上电测试 POST（开机自检）。
    
  \n\n  推荐：最简单的学习方案

  💡 建议：如果你想亲手搭一台计算机，不要从零开始焊 CPU，而是用现成的开发板，原理完全一样但省去了大量调试工作。

  
    推荐学习路径
    
      阶段工具学到什么
      
        入门Arduino Uno时钟、GPIO、中断、串口
        进阶树莓派 Pico双核 CPU、内存映射、USB
        高级树莓派 4完整 Linux、SATA、PCIe
        专业FPGA (Xilinx)自己实现 CPU 电路
      
    
  \n\nHARDWARE CIRCUIT GUIDE — CPU · RAM · DISK · IO · CLOCK — 2026\n\n
