# 13 - 会员与用户系统

> 如何设计安全的用户体系

---

## 📖 概述

本章介绍用户体系、权限管理、SSO单点登录设计。

---

## 🏗️ 一、用户体系

### 1.1 用户表设计

```sql
CREATE TABLE `t_user` (
  `id` BIGINT PRIMARY KEY,
  `username` VARCHAR(50) NOT NULL UNIQUE,
  `password` VARCHAR(100) NOT NULL,  -- bcrypt加密
  `phone` VARCHAR(20) UNIQUE,
  `email` VARCHAR(100) UNIQUE,
  `status` TINYINT DEFAULT 1,        -- 1=正常 0=禁用
  `user_type` TINYINT DEFAULT 1,     -- 1=普通 2=商家 3=管理员
  `created_at` DATETIME,
  `updated_at` DATETIME,
  INDEX `idx_phone` (`phone`),
  INDEX `idx_status` (`status`)
);
```

---

## 🔐 二、权限管理

### 2.1 RBAC模型

```
角色 ──► 权限
  │
  │ M:N
  ▼
用户 ──► 角色
```

### 2.2 Spring Security配置

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        return http
            .csrf().disable()
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/**").permitAll()
                .requestMatchers("/api/admin/**").hasRole("ADMIN")
                .anyRequest().authenticated()
            )
            .build();
    }
}
```

---

## 🔑 三、SSO单点登录

### 3.1 CAS流程

```
用户访问系统A ──► 未登录 ──► 重定向到SSO服务器
                                    │
                                    ▼
                              登录页面
                                    │
                              登录成功 ──► 颁发Ticket
                                    │
                                    ▼
                              重定向系统A ──► 验证Ticket ──► 获取用户信息
```

---

## 🔗 相关章节

- [系统架构设计](./01-系统架构设计.md)
- [安全防护体系](./17-安全防护体系.md)

---

*上一页：[支付系统设计](./12-支付系统设计.md) | 下一页：[接口性能优化](./14-接口性能优化.md)*
