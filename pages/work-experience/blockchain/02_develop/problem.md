### 没有配置默认的地址导出路径

### BTC的山寨币没有配置注解导致无法正常调用服务

### 新增区块链时、没有初始化数据导致调用不到服务

### tar包中没有删除以前版本导致问题：无法正确解析数据

### 热区前置和半热前置添加区别

### 区块通过过程中、调用jsonrpc经常报错、开启监听线程时、增加while循环确保可以获取区块

### BTC的山寨币监听线程暂时未正确启动

### OMNI和BTC使用的相同的区块链--获取充币信息时需要默认过滤数据